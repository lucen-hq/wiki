# Authenticaion Standard

For authentication the OIDC protocol is used.
Only Zitadel is permitted as identity provider.

Zitadel allows to quick move to a hosted solution in the future if needed.

Usage with terrafrorm is preferred.
